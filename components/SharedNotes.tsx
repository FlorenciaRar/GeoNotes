import React from "react";
import { View, Text } from "react-native";

export default function SharedNotes() {
  return (
    <View>
      <Text>Notas compartidas conmigo</Text>
      {/* Aca va un if
     - si no esta logeado pide log o regristro
     - si no muestra perfil */}
    </View>
  );
}
